<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ticket Confirmation - Nemesis</title>
</head>
<body style="margin: 0; padding: 0; background-color: {{colors.background}}; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;">
  <table width="100%" cellpadding="0" cellspacing="0" style="background-color: {{colors.background}};">
    <tr>
      <td align="center" style="padding: 40px 20px;">
        <table width="100%" cellpadding="0" cellspacing="0" style="max-width: 600px; background-color: {{colors.card}}; border-radius: 8px; border: 1px solid {{colors.border}};">
          {{> header}}

          <tr>
            <td style="padding: 32px 40px;">
              <h2 style="margin: 0 0 16px 0; font-size: 20px; font-weight: 600; color: {{colors.foreground}};">
                You're Going, {{firstName}}!
              </h2>

              <p style="margin: 0 0 24px 0; font-size: 15px; line-height: 1.6; color: {{colors.mutedForeground}};">
                Your tickets for <strong>{{#each order.items}}{{this.title}}{{#unless @last}}, {{/unless}}{{/each}}</strong> are confirmed.
              </p>

              <!-- Ticket Info -->
              <div style="padding: 20px; background-color: {{colors.muted}}; border-radius: 6px; margin-bottom: 24px; border: 1px dashed {{colors.border}};">
                {{#each order.items}}
                <div style="margin-bottom: 16px; {{#if @last}}margin-bottom: 0;{{/if}}">
                    <p style="margin: 0; font-weight: 600; color: {{../colors.foreground}};">{{this.title}}</p>
                    <p style="margin: 4px 0 0 0; font-size: 13px; color: {{../colors.mutedForeground}};">
                        Qty: {{this.quantity}} | Price: {{formatPrice this.price}}
                    </p>
                    {{#if this.ticketCode}}
                    <p style="margin: 8px 0 0 0; font-family: monospace; font-size: 12px; color: {{../colors.primary}};">
                        CODE: {{this.ticketCode}}
                    </p>
                    {{/if}}
                </div>
                {{/each}}
              </div>

              <div style="padding: 16px; background-color: {{colors.muted}}; border-radius: 6px; margin-bottom: 24px;">
                <p style="margin: 0; font-size: 14px; line-height: 1.6; color: {{colors.foreground}};">
                  <strong>Order Reference:</strong> #{{shortOrderId order._id}}<br>
                  <strong>Date:</strong> {{formatDate order.createdAt}}
                </p>
              </div>

              <table cellpadding="0" cellspacing="0">
                <tr>
                  <td align="center" style="border-radius: 6px; background-color: {{colors.primary}};">
                    <a href="{{clientUrl}}/orders/{{order._id}}" target="_blank" style="
                      display: inline-block;
                      padding: 12px 24px;
                      font-size: 14px;
                      font-weight: 500;
                      color: {{colors.primaryForeground}};
                      text-decoration: none;
                      border-radius: 6px;
                    ">View Ticket Details</a>
                  </td>
                </tr>
              </table>
            </td>
          </tr>

          {{> footer}}
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
